# ============================================
# ARCHIVO 3: render.yaml
# ============================================
services:
  - type: web
    name: sata-viajes
    env: docker
    plan: free
    healthCheckPath: /
    autoDeploy: true
